# Gemini / Orchestrator
GEMINI_API_KEY=AIzaSyCKwEWU8ctwjalNgAj-hXFMUC9ZN1cr8VE
# GEMINI_API_URL is optional if using SDK (default)
# GEMINI_API_URL=https://api.gemini.example/v1/agent
GEMINI_USE_SDK=true
GEMINI_MODEL=gemini-2.0-flash
GEMINI_IMAGE_MODEL=gemini-2.0-flash

# OpenRouter Configuration
USE_OPENROUTER=false
OPENROUTER_API_KEY=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
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Image generation (Leonardo / Midjourney etc.)
IMAGE_API_KEY=your_image_api_key
IMAGE_API_URL=https://api.imagegen.example/v1/generate
IMAGE_PROVIDER=leonardo

# Video generation (Luma / other)
VIDEO_API_KEY=your_video_api_key
VIDEO_API_URL=https://api.videogen.example/v1/animate

# If you want the orchestrator to require `ffmpeg` be present at startup set:
# VIDEO_REQUIRE_FFMPEG=true
#
# To instruct the video generator to produce local files instead of remote URLs
# set:
# VIDEO_OUTPUT_LOCAL=true

# Instagram (Facebook Graph) - use long-lived access token and IG user id
IG_USER_ID=your_ig_user_id
IG_ACCESS_TOKEN=your_instagram_graph_access_token

# YouTube Shorts (OAuth2)
YOUTUBE_CLIENT_SECRETS_FILE=client_secrets.json
YOUTUBE_TOKEN_FILE=youtube_token.json
YOUTUBE_PRIVACY_STATUS=private

# Toggle dry run (set to 'true' to avoid real API calls)
DRY_RUN=true

# Database
DATABASE_URL=sqlite:///generated_content.db
